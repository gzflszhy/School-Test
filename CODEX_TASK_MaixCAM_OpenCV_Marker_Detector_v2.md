# MaixCAM-Pro 端侧 OpenCV Marker 检测——需求与实现方案 v2.1

> 文档状态：方案冻结前评审稿，不代表已经完成代码或实机性能验收。
>
> 本版结合了原任务文档、`marker.3mf`、目标渲染图，以及新增的车辆运动和 LED 使用场景。原文档中的“直接实现”等语句仅作为旧任务草案内容，本次不执行代码实现。

## 1. 项目目标

在 Sipeed MaixCAM-Pro 上实现完全端侧运行的传统视觉 Marker 检测器：

- 生产实现使用 C++17、MaixCDK 和 OpenCV；
- 使用 MaixCDK Camera API 获取内置 GC4653 的图像，不使用 `cv::VideoCapture`；
- 不使用神经网络、NPU、YOLO、SIFT、ORB 或 AprilTag 替换目标；
- 摄像头安装在跟随车上，目标 Marker 安装在前方运动物体上；
- 两者沿大致相同方向运动，场景速度约 0.5 m/s；
- 使用环境为室内；
- Marker 黑色部分为实体，白色图案为主动发光 LED；
- 当前只完成视觉识别与测量输出，不实现小车运动控制；
- 端到端有效检测输出频率必须持续不低于 10 Hz；
- 为后续滤波和横向速度控制输出低延迟、带时间戳的目标中心。

## 2. 当前范围和非目标

### 2.1 本阶段包含

- 相机取流；
- Marker 搜索、确认和逐帧跟踪检测；
- 置信度、中心、包围框和检测状态输出；
- 检测耗时、端到端延迟及 FPS 统计；
- 运动、光照和负样本测试；
- 可选输出方向，但方向不作为跟随控制的必要条件。

### 2.2 本阶段不包含

- 车辆底盘控制；
- 横向速度控制器；
- 卡尔曼滤波器参数整定；
- 目标距离的毫米级估计；
- 6DoF 位姿；
- 多目标选择；
- 神经网络兜底。

视觉模块可以输出适合下游滤波的观测量，但不能把预测结果伪装成当前帧的真实检测结果。

## 3. 已知硬件条件

### 3.1 MaixCAM-Pro

MaixCAM-Pro 使用 SG2002，主 Linux 核运行为 1 GHz RISC-V C906，内存为 256 MB DDR3。MaixCDK 官方支持 MaixCAM-Pro、OpenCV 和 Camera API。

官方资料：

- [MaixCAM-Pro 硬件说明](https://github.com/sipeed/sipeed_wiki/blob/main/docs/hardware/en/maixcam/maixcam_pro.md)
- [MaixCDK Camera API](https://github.com/sipeed/MaixCDK/blob/main/components/vision/include/maix_camera.hpp)

### 3.2 GC4653 和默认镜头

根据 Sipeed 官方相机资料：

| 参数 | GC4653 / 官方模组 |
|---|---:|
| 原生分辨率 | 2560 × 1440 |
| 传感器规格 | 1/3 英寸 |
| 像元 | 2.0 μm |
| 快门 | Rolling Shutter，滚动快门 |
| 官方稳定帧率基线 | 2K 30 FPS、720P 60 FPS |
| 镜头焦距 | 约 3.05 mm |
| 光圈 | F2.5 |
| 水平视场角 | 约 81° |
| 垂直视场角 | 约 51° |
| 标称畸变 | 约 5% |

资料来源：[Sipeed MaixCAM Camera and Lens](https://github.com/sipeed/sipeed_wiki/blob/main/docs/hardware/en/maixcam/cameras.md)。

MaixPy 文档还列出了部分固件可请求 1280×720@80 FPS，但也提示部分相机配置在 80 FPS 下可能出现纹路或图像质量变化。因此本项目以 60 FPS 为稳定输入基线，80 FPS 只作为实机可选优化，不能写入硬性验收结论。

## 4. Marker 几何

从 `marker.3mf` 得到：

- 黑色主体约 115 × 120 × 15 mm；
- 白色 LED 图案覆盖约 80 × 80 mm；
- 白色图案由 6 个互不连通的组件构成：
  - 3 个约 30 × 30 mm 的大 L；
  - 1 个约 14 × 14 mm 的小 L；
  - 2 个约 8 × 8 mm 的小方块。

三个大 L 构成稳定的直角三角形布局，小 L 与两个方块形成唯一方向编码。

运行时不解析 3MF。多边形坐标、理论面积和相对位置应在代码中由统一的只读 Marker 几何配置生成模板，避免散落常量。

## 5. 对原方案的关键修订

### 5.1 分辨率改为近距离优先

车辆可以主动靠近 Marker，本项目不再为 1～3 m 远距离识别付出额外计算量。按照水平视场角 81°和针孔模型粗略估算，115 mm 宽目标在 640×360 画面中的尺寸约为：

| 距离 | 黑板宽度 | 8 mm 小灯宽度 |
|---:|---:|---:|
| 0.2 m | 约 215 px | 约 15 px |
| 0.3 m | 约 143 px | 约 10 px |
| 0.5 m | 约 86 px | 约 6 px |
| 0.8 m | 约 54 px | 约 4 px |
| 1.0 m | 约 43 px | 约 3 px |

这是未考虑畸变、模糊和实际安装偏差的近似值。320×240 虽然更快，但在 0.5 m 处两个最小 LED 可能只剩约 3 px，运动和阈值处理后容易丢失。

因此默认设计确定为：

- GC4653 以 640×360、60 FPS 取流和处理，保持传感器的 16:9 视场；
- 全图搜索和 ROI 精细验证都在同一幅 640×360 灰度图上进行，避免双分辨率、缩放和跨通道同步开销；
- 进入跟踪状态后只处理局部 ROI；
- 建议设计工作区间先定为 0.2～0.8 m；
- 0.8～1.0 m 作为可探测但不承诺六组件完整识别的边缘区间；
- 只有真实比赛证明必须在更远处首次捕获，才增加 1280×720 搜索模式。

车辆“可以尽量靠近”不等于算法支持任意距离。最终验收必须写明最大可靠距离。相机为手动对焦 M12 镜头，装车后应把焦点调在主要跟随距离，而不是默认使用出厂焦点。

### 5.2 LED 图案应成为主视觉信息

原方案把黑色外板作为主路径、白色图案作为 fallback。实际白色部分是主动 LED 后，应改为：

1. LED 三大 L 的亮区域拓扑作为主候选路径；
2. 黑色实体板的暗矩形作为联合验证或备用候选；
3. 小 L 和两个小方块用于高置信度身份与方向验证；
4. 在 LED 过曝膨胀时使用允许范围 mask、距离比例和灰度剖面，而不是要求轮廓与理论多边形逐像素一致。

原因是室内暗背景下黑板可能与背景粘连，而 LED 通常具有更稳定的局部对比度。

### 5.3 分离“可跟随检测”和“完整身份检测”

为了避免远距离时两个 8 mm 小灯只有几像素而导致掉检，定义两级检测结果：

#### TRACKABLE

- 三个大 L 的尺度、距离、直角关系和亮度对比可靠；
- 黑板暗区或更多 LED 组件提供至少一种附加验证；
- 可以稳定输出中心和 bbox；
- 满足当前跟随任务需要；
- 方向可能标记为 unknown。

#### FULL_ID

- 六组件中的绝大多数都可靠匹配；
- 小 L 和两个方块足以确定唯一方向；
- 置信度更高；
- 可输出 0/90/180/270 离散方向。

下游跟随不应强制等待 FULL_ID。只有 TRACKABLE 也可以提供观测，但需要在结果中明确质量等级。

### 5.4 不再假定目标永远像素级正对

两车运动中会产生车体振动、偏航、转弯和安装误差。即使机械上大致正对，图像也不应被假定为始终轴对齐。

本阶段仍不做 3D 位姿和 `solvePnP`，但允许：

- `minAreaRect` 获取旋转矩形；
- 基于三大 L 的中心建立平面相似变换；
- 对 ROI 做平移、旋转和统一尺度归一化；

这不是完整透视恢复。如果实测侧视角导致模板显著变形，再评估是否需要受限 homography。不能仅因为旧文档禁止透视就忽略真实数据中的变形。

## 6. 相机配置策略

### 6.1 默认配置

- 传感器：GC4653；
- 输入：640×360；
- 请求帧率：60 FPS；
- 格式优先级：`FMT_GRAYSCALE` → NV21/YUV 的 Y 平面 → RGB/BGR 后单次转灰度；
- `buff_num`：从 1 开始测试，若出现丢帧或不稳定再改为 2；
- 生产模式关闭屏幕显示和逐帧文件保存；
- 启动后跳过至少 20～30 帧，等待 AE 稳定。

官方文档指出降低 `buff_num` 有利于降低采集延迟，但 MaixCAM 内部仍至少存在双缓冲，最低采集延迟约 30 ms 以上。因此控制场景不仅要看 FPS，还必须记录画面时间戳到检测输出的延迟。

### 6.2 曝光与 LED

GC4653 是滚动快门。运动和 LED PWM 都可能产生：

- 运动拖影；
- 斜变形；
- LED 明暗条带；
- 某些帧中小灯消失；
- 过曝光晕导致相邻组件粘连。

建议流程：

1. 开机先用自动曝光完成场景适应；
2. 检测到 LED 后逐步限制最大曝光时间；
3. 以 1～2 ms 作为首轮实测区间，而不是直接写成最终固定值；
4. 根据亮度提高适量模拟/数字增益；
5. 设置过曝面积和光晕宽度监控，避免阈值把 LED 膨胀成一整块；
6. 如果 LED 使用 PWM，优先改为常亮直流或足够高频的 PWM，再根据曝光时间实测条带。

曝光参数必须进入配置和 benchmark 报告。

当前 LED 驱动方式和 PWM 频率未知，因此不能在设计阶段假定 LED 常亮。首次实机测试必须增加 LED 时域诊断：

- 固定 Marker 和相机，分别以 30/60 FPS 录制灰度序列；
- 扫描若干曝光时间，记录每个 LED 区域逐帧平均亮度、最低亮度和饱和比例；
- 检查图像行方向是否出现周期性明暗条纹；
- 检查是否存在整帧 LED 熄灭或部分组件消失；
- 将诊断结果写入 benchmark 报告，再决定固定曝光、AE 上限和是否尝试 80 FPS。

软件侧采用以下容错，但不承诺掩盖严重的低频 PWM：

- 阈值使用相对局部对比而不是固定绝对灰度；
- Large-L 允许被少量暗条带切断后再通过小核 close 恢复；
- TRACK 状态允许单帧 miss，但该帧仍必须输出 `found=false`；
- 下游滤波器可以利用时间戳短时预测；
- 不默认对多帧图像做最大值叠加，避免运动目标产生重影和中心偏移。

如果 LED 在整个曝光窗口内确实完全熄灭，单帧图像中不存在可恢复的信息，传统视觉算法无法凭空识别。此时必须提高 PWM 频率、改为直流常亮，或在运动模糊允许范围内增加曝光时间。

## 7. 检测链路

```text
MaixCDK Camera read
        ↓
取得灰度或 Y 平面
        ↓
SEARCH_FULL / TRACK_ROI 状态机
        ↓
亮 LED 候选提取
        ↓
三个 Large-L 的尺寸、距离、直角拓扑组合
        ↓
推算目标中心、旋转矩形和尺度
        ↓
原分辨率 ROI 精细验证
        ↓
黑板暗区 + 六组件模板 + 局部对比联合评分
        ↓
TRACKABLE / FULL_ID / NOT_FOUND
        ↓
输出带时间戳的观测
```

### 7.1 全图搜索预处理

- 直接使用相机输出的 640×360 灰度图，不做整帧缩放；
- 使用亮度高分位数、Otsu 和可配置的局部对比阈值生成 LED mask；
- 仅使用一次小核 morphology 处理离散噪点；
- 不默认使用全图 CLAHE 或大窗口 adaptive threshold；
- 对明显饱和区域记录饱和比例。

阈值不能只用固定灰度值，因为 LED 亮度、曝光和环境照明会变化。

### 7.2 Large-L 候选

对连通域计算：

- bbox 和旋转 bbox；
- 面积与填充率；
- 宽高比；
- 轮廓顶点和凹度；
- Hu moment / `matchShapes` 分数；
- 区域亮度及其相对周围的对比度。

LED 光晕会改变理论面积，因此形状评分采用范围和连续分数，不能要求固定面积相等。

### 7.3 三点拓扑组合

从最多若干个 Large-L 候选中寻找尺寸相近的三元组：

- 两条短边中心距离约为 L 尺寸的 1.98 倍；
- 对角距离约为 L 尺寸的 2.80 倍；
- 满足近似勾股关系；
- 三个组件亮度和尺度相近；
- 在连续帧中中心移动符合有限速度约束。

从三点关系得到 Marker 中心、图像内旋转和预计外框范围。

### 7.4 ROI 精细验证

在 640×360 当前帧的候选 ROI 内：

- 使用亚像素或原尺度轮廓重新定位三大 L；
- 以相似变换归一化到 canonical canvas；
- 对 0/90/180/270 模板分别评分；
- 统计每个组件 coverage；
- 计算 LED 区与黑色参考区的灰度对比；
- 检查多余亮区、组件粘连和缺失；
- 检查黑色实体板的暗区面积与整体形状。

建议 confidence 由以下部分构成，最终权重由实拍数据标定：

- 三大 L 拓扑与形状：40%；
- 六组件模板 coverage：25%；
- LED/黑板局部对比：15%；
- 黑色实体板几何：10%；
- 时间连续性：10%。

时间连续性只能调整置信度，不能单独把 miss 变成 found。

## 8. SEARCH / TRACK 状态机

### SEARCH_FULL

- 在整幅 640×360 图像上运行 LED 主路径；
- LED 路径失败时再运行黑板暗矩形备用路径；
- 对候选直接进行 640×360 ROI 验证；
- 连续两帧候选位置相符并达到 strong threshold 后进入 TRACK_ROI。

### TRACK_ROI

- 使用上一帧中心和短时间常速度预测确定搜索 ROI；
- ROI 初始扩大为上次 bbox 的 1.5～2.0 倍；
- 每一帧仍执行图案检测和验证；
- 预测只决定搜索位置，不直接生成 found；
- 连续丢失两帧后扩大 ROI；
- 连续丢失三帧后回到 SEARCH_FULL。

为避免低分辨率目标在 SEARCH 与 TRACK 阈值之间反复跳变，需要设置进入、保持和退出三档迟滞阈值。

## 9. 输出接口

每个已处理相机帧输出一条 `DetectionResult`：

- `found`；
- `quality = NOT_FOUND / TRACKABLE / FULL_ID`；
- `confidence`；
- `bbox_x, bbox_y, bbox_w, bbox_h`，坐标基于 640×360 相机图像；
- `center_x, center_y`；
- `error_x_px = center_x - optical_center_x`；
- `error_x_norm = error_x_px / (frame_width / 2)`；
- `orientation = 0/90/180/270/unknown`；
- `capture_timestamp`；
- `output_timestamp`；
- `capture_to_output_latency_ms`；
- `processing_ms`；
- `effective_detection_fps`；
- `state = SEARCH_FULL / TRACK_ROI`。

下游滤波和横向速度控制优先使用 `error_x_norm`、时间戳、confidence 和 quality。`center_x` 必须基于标定后的光学中心，而不是默认把图像几何中心永远视为光学中心。

## 10. 性能指标

### 10.1 硬性指标

- 每个相机帧从获取到检测输出形成完整闭环；
- 有效检测输出持续不低于 10 Hz；
- 不得用相机采集 FPS 代替实际检测 FPS；
- P95 端到端延迟小于 100 ms；
- 连续运行至少 10 分钟后仍满足指标；
- 生产测试关闭 GUI、逐帧保存和高频日志。

### 10.2 开发目标

跟随控制对延迟敏感，因此开发目标应高于最低要求：

- TRACK_ROI：目标 20 Hz 以上；
- SEARCH_FULL：目标 12～15 Hz 以上；
- 常态 P95 端到端延迟争取不超过 60～70 ms；
- 使用最新帧策略，避免处理队列累积造成“FPS 足够但画面很旧”。

### 10.3 必测阶段

1. Camera read 基线；
2. 640×360 灰度/Y 平面接入开销；
3. SEARCH_FULL 主路径；
4. ROI 验证；
5. TRACK_ROI；
6. LED 过曝、不同曝光和 PWM；
7. 0.5 m/s 运动测试；
8. 10 分钟温升与长时间测试。

每阶段记录 mean、P50、P95、P99、1% low FPS 和丢帧数。

## 11. 准确率定义

在工作区间确定后，分别评测：

- frame-level recall；
- track continuity：连续运动中最长丢检帧数；
- false positive rate；
- 中心点误差；
- capture-to-output latency；
- 重新捕获时间；
- TRACKABLE 与 FULL_ID 的比例。

建议首轮验收目标：

- 完整可见且满足最小像素尺寸时，TRACKABLE recall ≥ 97%；
- 负样本强确认误检率 ≤ 0.1%；
- 连续运动中不出现超过 3 个处理帧的非遮挡掉检；
- 中心点相对人工标注的 P95 误差 ≤ bbox 宽度的 3%；
- 失去目标后重新进入画面，0.3 s 内重新捕获。

这些是待实测目标，不是当前已经取得的结果。没有数据时不得填写虚假准确率。

## 12. 测试数据矩阵

### 正样本

- 不同跟随距离；
- Marker 位于画面左、中、右；
- 两车静止、单车移动、两车同时移动；
- 0～0.5 m/s 速度；
- 小车振动和短时转弯；
- LED 不同亮度；
- LED 驱动未知条件下的 30/60 FPS 与多档曝光扫描；
- 明亮室内、普通室内、较暗室内；
- 轻微偏航、俯仰和画面内旋转；
- 短时离开画面后重新出现。

### 负样本

- 普通白色灯条、方灯、L 形灯；
- 反光金属和高亮标识；
- 黑色矩形物体；
- 文字、二维码、屏幕；
- 其他机器人灯光；
- 无 Marker 的比赛场地录像。

### 关于遮挡

“是否允许遮挡”指 Marker 是否可能被其他机器人、结构件、人物部分挡住，或者跑到图像边缘导致一部分不在画面内。

本版默认：

- 正常验收要求 Marker 完整可见；
- 部分遮挡时允许短暂返回 NOT_FOUND；
- 下游滤波器可以短时预测，但视觉模块不能把预测当作检测；
- 不承诺只看见一两个 L 时仍能可靠确认目标。

## 13. 软件结构建议

```text
maixcam_marker_detector/
├── app.yaml
├── main/
│   ├── CMakeLists.txt
│   ├── include/
│   │   ├── detector_config.hpp
│   │   ├── detection_result.hpp
│   │   ├── marker_geometry.hpp
│   │   ├── marker_template.hpp
│   │   ├── marker_detector.hpp
│   │   ├── detector_state.hpp
│   │   ├── maix_camera_source.hpp
│   │   └── benchmark.hpp
│   └── src/
│       ├── main.cpp
│       ├── marker_geometry.cpp
│       ├── marker_template.cpp
│       ├── marker_detector.cpp
│       ├── maix_camera_source.cpp
│       └── benchmark.cpp
├── tools/
│   ├── offline_evaluator/
│   └── annotation_format.md
└── README.md
```

所有阈值必须放入 `DetectorConfig`，包含：

- 相机分辨率、FPS、buffer 数；
- 曝光、增益和 AE 策略；
- 搜索缩放尺寸；
- 最小目标和最小组件像素尺寸；
- LED 阈值与饱和阈值；
- contour、形状、三点拓扑范围；
- canonical canvas 尺寸；
- TRACKABLE/FULL_ID 各项权重和阈值；
- ROI 放大比例；
- confirm/lost 帧数；
- debug、benchmark、failure recorder 开关。

## 14. 实施顺序

1. 确认当前 MaixCDK 版本和 GC4653 Camera API；
2. 完成 640×360@60 的 Camera 基线和时间戳；
3. 验证 grayscale、NV21 Y 平面和 RGB 转灰度三种路径；
4. 确认全程不必要的图像复制和颜色转换已经消除；
5. 根据 3MF 固定坐标生成 Marker 模板并自检；
6. 完成 LED 三大 L 主候选路径；
7. 完成 ROI 的 TRACKABLE 验证；
8. 完成 FULL_ID 六组件验证和方向；
9. 加入黑板备用路径；
10. 加入 SEARCH/TRACK 状态机和最新帧策略；
11. 加入 benchmark 和低频失败样本记录；
12. 采集真实动态数据并标注；
13. 调整曝光、阈值和迟滞；
14. 完成 10 Hz、准确率和长时间实机验收。

## 15. 实现开始前仍需确认的现场参数

以下信息不会阻止搭建代码框架，但会决定最终分辨率、曝光和验收范围：

1. 正常、最近和最远跟随距离；
2. LED 驱动方式暂时未知；实机阶段通过亮度序列和条纹测试判断是否为影响识别的 PWM；
3. Marker 是否通常能完整出现在画面内；
4. 两车转弯时 Marker 相对相机的最大偏航角；
5. 0.5 m/s 是两车各自速度上限，还是两者之间可能达到的相对速度；
6. GC4653 是否使用 Sipeed 官方 3.05 mm 镜头，安装后有没有更换镜头。

在这些参数未知时，首轮实现按以下假设推进：

- 主要跟随距离 0.2～0.8 m，允许车辆主动靠近；
- 官方 3.05 mm 镜头；
- Marker 正常情况下完整可见；
- 相对偏航角不超过约 15°；
- LED 驱动未知；首轮算法允许亮度波动，但如果出现整帧熄灭则转为硬件/曝光问题处理；
- 最大相对横向速度按 0.5 m/s 设计。

## 16. 完成定义

- [ ] 使用 MaixCDK Camera API 获取 GC4653 图像；
- [ ] 最终部署为 C++17；
- [ ] 不使用神经网络或通用特征点方案；
- [ ] 使用 3MF 六组件固定几何；
- [ ] 640×360 低延迟生产取流与检测链路完成；
- [ ] LED 三大 L 主路径完成；
- [ ] 黑色实体板备用/联合验证完成；
- [ ] TRACKABLE 与 FULL_ID 两级结果完成；
- [ ] SEARCH_FULL 与 TRACK_ROI 完成；
- [ ] 每个处理帧输出时间戳、中心、bbox、confidence、quality、延迟和 FPS；
- [ ] debug 和失败样本保存默认关闭；
- [ ] Release 构建成功；
- [ ] 真实 GC4653 实机 benchmark 完成；
- [ ] 连续运行至少 10 分钟仍 ≥10 Hz；
- [ ] 实测准确率、误检率和中心误差有真实数据支撑；
- [ ] 未达到的指标明确记录，不伪造结果。
